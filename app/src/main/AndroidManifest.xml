<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- ウィジェット1分更新用 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- バッテリー最適化除外リクエスト用（OEMの独自電力管理によるアラーム遮断を防ぐ） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Scombzwidgets"
            android:usesCleartextTraffic="true"
            android:hardwareAccelerated="true"
            tools:targetApi="31">

        <!-- メインアクティビティ -->
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.Scombzwidgets">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ログインアクティビティ (WebView専用テーマ + ハードウェアアクセラレーション) -->
        <activity
                android:name=".LoginActivity"
                android:exported="false"
                android:label="ScombZ ログイン"
                android:hardwareAccelerated="true"
                android:theme="@style/Theme.Scombzwidgets.Login" />

        <!-- 時間割ウィジェット -->
        <receiver
                android:name=".widget.TimetableWidgetReceiver"
                android:label="@string/widget_timetable_label"
                android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/widget_timetable_info" />
        </receiver>

        <!-- 課題ウィジェット -->
        <receiver
                android:name=".widget.AssignmentWidgetReceiver"
                android:label="@string/widget_assignment_label"
                android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/widget_assignment_info" />
        </receiver>

        <!-- バス時刻ウィジェット -->
        <receiver
                android:name=".widget.BusWidgetReceiver"
                android:label="@string/widget_bus_label"
                android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/widget_bus_info" />
        </receiver>

        <!-- ウィジェット1分ごとUI更新レシーバー -->
        <!-- exported="true" は BOOT_COMPLETED / MY_PACKAGE_REPLACED（システム送信）を受け取るために必要 -->
        <receiver
                android:name=".widget.WidgetUpdateReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="com.testapp.scombz_widgets.WIDGET_TICK" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
